
//finding a FIBONACCI SEREIS .

import java.util.Scanner;

public class clg_prog_1 {
   public static void main(String[] args) {
      // Scanner scanner = new Scanner(System.in);
      // System.out.print("Enter the number of terms: ");
      // int numTerms = scanner.nextInt();
      // scanner.close();

      // // Initializing the first two Fibonacci numbers
      // int previousNum = 0, currentNum = 1;

      // // Print the first two F ibonacci numbers
      // System.out.print(previousNum + " " + currentNum);

      // // Loop to generate subsequent Fibonacci numbers
      // for (int i = 2; i < numTerms; i++) {
      // int nextNum = previousNum + currentNum;
      // System.out.print(" " + nextNum);
      // previousNum = currentNum;
      // currentNum = nextNum;
      // }

      int a =6;
      int b =a++;
      System.out.println(a);
      System.out.println(b);
      a=6;
      b=++a;
       System.out.println(a);
      System.out.println(b);
   }
}